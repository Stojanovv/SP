#include <iostream>
#include<cmath>
#include<iomanip>
using namespace std;

int main() {
   int n;
   cin>>n;
   int niza[n];
    for (int i = 0; i < n; ++i) {
        cin>>niza[i];
    }
    int k;
    cin>>k;
    int temp1,temp2;
    for (int i = 0; i < n; ++i) {
        if (i==k){
            temp1=niza[i-1];
            niza[i-1]=niza[i];
            niza[i]=temp1;
        }
        if (i==n-k){
            temp1=niza[i-1];
            niza[i-1]=niza[i];
            niza[i]=temp1;
        }
    }
    for (int i = 0; i < n; ++i) {
        cout<<niza[i]<<" ";
    }

    return 0;
}
