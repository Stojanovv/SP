#include <iostream>
#include<iomanip>
using namespace std;
int main() {
    int n;
    double sredna,suma=0.0,min=1000.0,k,x;
    double niza[100];
    cin>>n;
    for (int i = 0; i < n; ++i) {
        cin>>niza[i];

    }
    for (int i = 0; i < n; ++i) {
        suma+=niza[i];

    }
    sredna=suma/n;
    x=niza[0];
    n=niza[0]-sredna;
    for (int i = 0; i < n; ++i) {
        double razlika=niza[i]-sredna;
        if(razlika<n){
            n=razlika;
            x=niza[i];
        }




    }
    cout<<fixed<<setprecision(2)<<x;
}
