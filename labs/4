#include <iostream>
#include<iomanip>
#include<vector>
#include<algorithm>

using namespace std;


int main() {
    int n,k;
    cin>>n;
    int a[n],b[k];
    for (int i = 0; i < n; ++i) {
        cin>>a[i];
    }
    cin>>k;
    for (int i = 0; i < k; ++i) {
        cin>>b[k];

    }
    int c[n+k];
    for (int i = 0; i < n; ++i) {
        c[n+k]=a[i];

    }
    for (int i = 0; i < n+k; ++i) {
        c[n+k]=b[i];

    }
    sort(c,c+n+k);
    for (int i = 0; i < n+k; ++i) {
        cout<<c[n+k];

    }




    return 0;
}
