#include <iostream>
#include<cstring>
#include<iomanip>
using namespace std;

int main(){
   int n;
   cin>>n;
   int niza[n];
    for (int i = 0; i < n; ++i) {
        cin>>niza[i];
    }
    int broj;
    for (int i = 0; i < n; i+=2) {
        broj=niza[i]*10+niza[i+1];
        if (i==n-1){
            broj=niza[i];
        }
        if (broj%2==0 || broj%3==0 || broj%5==0 || broj%77==0){
            cout<<"brojot "<<broj<<" NE e prost"<<endl;
        }else if (broj%2==1){
            cout<<"brojot "<<broj<<" e prost"<<endl;
        }
    }
    }
